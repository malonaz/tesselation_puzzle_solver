#include "imageProcessor.h"


/**
 * Helper function which rotates the given shape's coordinates so that all sides 
 * of the given shape are vertical or horizontal.
 */
std::vector<int> rotate_shape(std::vector<int> &shape){
  std::vector<int> rotated_shape;
  return rotated_shape;
}

std::vector< std::vector<int> > rotate_shapes(std::vector< std::vector<int> > &shapes){
  std::vector< std::vector<int> > rotated_shapes;
  return rotated_shapes;
}





